/**
 * AI Provider Enum
 * Defines supported AI providers for the application
 */
export enum AIProvider {
  OPENAI = 'openai',
  GEMINI = 'gemini',
}

export enum AIProviderName {
  OPENAI = 'OpenAI',
  GEMINI = 'Gemini',
}
